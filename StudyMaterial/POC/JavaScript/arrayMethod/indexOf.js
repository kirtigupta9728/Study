// The indexOf() method is used when we want to find the first
//  occurrence of a specified element and the element is known.

// The findIndex() method is used when we want to specify a 
// particular condition to that element. 

// JavaScript indexOf() method takes direct element is 
// specified so it is useful when we are working with array of objects.

// JavaScript findIndex() method takes a callback 
// function which returns the value of the element whose 
// index is to be foun


let array = ["Geeks", "GFG", "geeks", "Geeks"];
 
console.log("Array: [" + array + "]");
 
console.log("First index of 'Geeks' is "
    + array.indexOf('Geeks'));