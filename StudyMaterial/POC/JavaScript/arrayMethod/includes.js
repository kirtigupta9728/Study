// This method is used to know whether a particular element is 
// present in the array or not and accordingly, it returns true 
// or false i.e, if the element is present, then it returns 
// true otherwise false

let arr = [1, 2, 3, 4, 5];
let a = arr.includes(2);
 
console.log(a);
